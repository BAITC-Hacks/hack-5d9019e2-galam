Read `MASTER_SPEC_AKIM_5_HOURS.md` completely and treat it as the source of truth.
Inspect the repository before editing.

Start ONLY with Milestones 1 and 2:
1) project foundation;
2) deterministic official simulation engine.

Do not build visual polish or AI yet.

Implement:
- typed official dataset loading from `official-dataset.json`;
- schema validation;
- lag/effect calculation;
- city- and district-scope effects;
- official synergies;
- official incompatibilities;
- strict Governance Challenge validation;
- official district/city/final scoring formulas;
- mandatory regression tests from the Master Spec.

Run tests, typecheck and production build.
Fix all failures.
Then STOP.

Report:
- what was implemented;
- exact commands run;
- test results;
- production build result;
- any discrepancy in the source-provided example score;
- files changed;
- any risks or ambiguities.

Do not proceed to later milestones until I approve.
